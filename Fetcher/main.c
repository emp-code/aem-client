#include "../all-ears.h"

int main(void) {
	if (sodium_init() != 0) {puts("Failed sodium_init()"); return EXIT_FAILURE;}

//	allears_init(host, lenHost, usk);

//	allears_address_lookup("test");

//	allears_free();

	return 0;
}
